ignore-workspace-root-check=true

engine-strict=true

# Package manager: pnpm ONLY — see .agents/rules/000-package-manager.md
# ignore-workspace-root-check is required for shadcn CLI which spawns its own pnpm subprocess.
# This does NOT override the preinstall only-allow guard for human usage.
